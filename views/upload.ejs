<!DOCTYPE html>
<html>

<head>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <% include scripts.ejs %>
</head>

<body>
    <div class="upload-modal">
        <div class="title"><i class="fa fa-cloud-upload"></i> Upload</div>
        <form id="uploadForm" enctype="multipart/form-data" action="/api/photo" method="post" class="form-group">
            <fieldset class="form-group">
                <label for="userPhoto"><i class="fa fa-photo"></i> Select a photo to upload</label>
                <input type="file" accept="image/*" name="userPhoto" class="form-control" placeholder="Select a photo to upload" />
                <hr />
                <label for="userCaption"><i class="fa fa-pencil"></i> Enter a caption for the photo</label>
                <textarea rows="3"  name="userCaption" class="form-control" placeholder="Enter a caption for the photo"></textarea>
                <hr />
                <small class="text-danger text-muted">Warning: Once uploaded, this image will be displayed on the dashboard. Contact the event organizer if you want to delete the photo.</small>
                <hr />
                <button type="submit" name="submit" class="btn btn-violet"><i class="fa fa-cloud-upload"></i> Upload</button>
            </fieldset>
            <% if(message) { %>
                <fieldset>
                    <% if(error) { %>
                        <div class="alert alert-danger">
                            <%= message %>
                        </div>
                        <% } else { %>
                            <div class="alert alert-success">
                                <%= message %>
                            </div>
                            <% } %>
                </fieldset>
                <% } %>
        </form>
    </div>
</body>

</html>
